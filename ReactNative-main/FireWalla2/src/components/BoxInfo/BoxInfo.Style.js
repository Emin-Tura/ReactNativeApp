import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  boxInfo: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: 10,
  },
  innerBoxInfo: {
    marginTop: 2,
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
});
