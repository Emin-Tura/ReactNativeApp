import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  counter: {
    backgroundColor: '#77838D',
    opacity: 0.7,
    width: 50,
    height: 50,
    borderRadius: 50,
    textAlign: 'center',
    alignItems: 'center',
    paddingTop: 14,
    fontSize: 16,
    color: '#ffffff',
  },
});
